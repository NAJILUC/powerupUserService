server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userservice
    username: root
    password: sasa
  application:
    name: user-service
  

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
appDescription: "Pragma Powerup"
appVersion: "1.0.0"
